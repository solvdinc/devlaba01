import React from 'react';
import PropTypes from 'prop-types';
import { base } from './Traffic.module.scss';

const TrafficBase = (props) => {
  return <div className={base} />;
};

TrafficBase.propTypes = {};

export default TrafficBase;
